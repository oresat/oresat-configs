"""Implementation for the oresat-configs script subcommands.

When using oresat-configs as a standalone command (e.g. `oresat-configs cards`)
this is the module where each of those are implemented. The submodules here
aren't really expected to be imported by other modules so in that sense
everything here is standalone.

main.py ties everything together, see the instructions there for how to add a
new subcommand.
"""
